# Pontos de futebol
def football_points(wins, draws, losses):
  return wins * 3 + draws * 1 + losses * 0

# Conversão de string para inteiro e vice-vers
def to_int(txt):
	return int(txt)
def to_str(num):
  return str(num)
